// The Live Campaigns screen (scenes/main/LiveCampaign.tsx) shows a subset
// of the canonical campaign list - see data/campaigns.ts, the single
// source of truth every campaign-related screen (including the Campaign
// Details `/campaign/[id]` lookup) now shares. See
// docs/screen/live-campaign/README.md "Scope notes" for the mock content
// this represents.
export { liveCampaigns } from './campaigns';
